{
  "name": "rspack",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rspack serve",
    "build": "rspack build",
    "serve": "serve -s ./dist -l 3003",
    "test": "playwright test"
  },
  "devDependencies": {
    "@rspack/cli": "latest",
    "typescript": "latest",
    "json-loader": "0.5.7"
  }
}
